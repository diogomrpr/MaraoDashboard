{
  "config": {
    "step": {
      "user": {
        "title": "Marao Dashboard",
        "description": "Install the Marao Dashboard builder, cards, and theme. The dashboard cards are provided by Marao."
      }
    },
    "abort": {
      "already_configured": "Marao Dashboard is already configured."
    }
  },
  "issues": {
    "legacy_vendor_resources": {
      "title": "Remove old Marao frontend resources",
      "description": "Marao now loads its own frontend from the integration. Remove these legacy Marao or bundled-card entries from your YAML Lovelace resources, then restart Home Assistant: {resources}"
    }
  },
  "services": {
    "generate_dashboard": {
      "name": "Generate dashboard",
      "description": "Generate the Marao Dashboard YAML dashboard from dashboard.json.",
      "fields": {
        "config_path": {
          "name": "Config path",
          "description": "Path to the dashboard JSON config, relative to /config unless absolute."
        },
        "dashboard_key": {
          "name": "Dashboard key",
          "description": "Optional Lovelace dashboard key override."
        },
        "dry_run": {
          "name": "Dry run",
          "description": "Validate and report planned files without writing YAML or patching configuration.yaml."
        }
      }
    }
  }
}
